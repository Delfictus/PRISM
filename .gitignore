# Environment variables with API keys (NEVER commit!)
.env
.env.local
.env.*.local

# Cargo lock for libraries
Cargo.lock

# Large binary artifacts and build outputs
deps/
target/
*.rlib
*.d
libonnxruntime*.so*
comprehensive_gpu_benchmark
__pycache__/
telemetry/
