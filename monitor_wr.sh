#!/bin/bash
# PRISM World Record Live Monitor
# Shows latest progress and GPU stats

LOG_FILE="${1:-results/logs/wr_run.log}"

clear
echo "╔═══════════════════════════════════════════════════════════╗"
echo "║        PRISM World Record - Live Monitor                  ║"
echo "╚═══════════════════════════════════════════════════════════╝"
echo ""

while true; do
    # Move cursor to top
    tput cup 4 0

    echo "─────────────────────────────────────────────────────────────"
    echo "📊 LATEST PROGRESS (Last 30 lines):"
    echo "─────────────────────────────────────────────────────────────"

    if [ -f "$LOG_FILE" ]; then
        tail -30 "$LOG_FILE" | grep -E "PHASE|colors|🎯|✅|FINAL" || echo "Waiting for progress..."
    else
        echo "Waiting for log file: $LOG_FILE"
    fi

    echo ""
    echo "─────────────────────────────────────────────────────────────"
    echo "🎮 GPU STATUS:"
    echo "─────────────────────────────────────────────────────────────"
    nvidia-smi --query-gpu=name,utilization.gpu,memory.used,memory.total,temperature.gpu --format=csv,noheader | \
        awk -F', ' '{printf "GPU: %s\nUsage: %s | VRAM: %s / %s | Temp: %s\n", $1, $2, $3, $4, $5}'

    echo ""
    echo "─────────────────────────────────────────────────────────────"
    echo "Press Ctrl+C to exit | Refreshing every 2 seconds..."

    sleep 2
done
